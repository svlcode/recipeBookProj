import { RecipeListComponent } from "./recipe-list.component";

describe('RecipeListComponent', () => {
  let fixture: RecipeListComponent;

  beforeEach(() => {
    fixture = new RecipeListComponent();
  })

  describe('setup component', () => {
    it('should create', () => {
      
    })
  })
});
