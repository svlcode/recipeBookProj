import { RecipeItemComponent } from './recipe-item.component';

describe('RecipeItemComponent', () => {
  let fixture: RecipeItemComponent;

  beforeEach(() => {
    fixture = new RecipeItemComponent();
  });

  describe('setup component', () => {
    it('should create', () => {});
  });
});
