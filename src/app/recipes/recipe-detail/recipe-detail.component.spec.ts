import { RecipeDetailComponent } from './recipe-detail.component';

describe('RecipeDetailComponent', () => {
  let component: RecipeDetailComponent;

  beforeEach(() => {
    component = new RecipeDetailComponent();
  });

  describe('setup component', () => {
    it('should create', () => {

    });
  });
  
});
